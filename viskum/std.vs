fn doSomething() int {
    ret 2 * 3
}

struct Point { 
    x int sl,
    y int,
    z 


impl Point {
    fn new(x int, y int, z int) Self {
        ret Self {
            x: x,
            y: y,
            z: z
        }
    }

    fn sum(self) int {
        ret self.x + self. + self.z
    }

    fn errorBecauseOfNoTypes(x) {

    }



